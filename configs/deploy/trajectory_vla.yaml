includes:
  - ../base.yaml
  - ../policies/trajectory_vla.yaml

policy:
  checkpoint: outputs/train/trajectory_vla_weldpath_relative_v1/checkpoints/last/pretrained_model

training:
  resume: false

deployment:
  episodes: 5
