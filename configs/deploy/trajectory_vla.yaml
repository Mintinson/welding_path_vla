includes:
  - ../policies/trajectory_vla.yaml

policy:
  checkpoint: outputs/train/trajectory_vla_weldpath_v1/checkpoints/005000/pretrained_model

training:
  resume: false

deployment:
  episodes: 5
