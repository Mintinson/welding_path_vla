includes:
  - act.yaml

training:
  output_dir: outputs/train/act_weldpath_relative_v1_a100
  # 每卡 batch 32，双卡全局 batch 64；约完整遍历训练集一次。
  batch_size: 32
  steps: 57407
  num_workers: 8
  eval_steps: 5000
  save_freq: 10000
  resume: false
