# VLA 真机数据采集和模型部署框架搭建

这是一个针对 VLA (Vision Language Model)在工业焊接机器人上进行数据采集，模型训练和模型部署的项目。使用 `pixi` 管理环境和执行常用命令。基于 `lerobot` 机器人学习框架。

使用的机器人为 Huayan Elfin5-Pro.

代码风格要求：
1. 代码尽量简洁优雅，能用10行完成一个功能，不要写到100行。
2. 不要过度防御性编程。
3. 命名规范：尽量避免使用 _ 开头给函数或者变量命名。这会降低程序的可读性，导致程序太丑。直接使用普通的命名，或者对于常用函数，抽象出来归到另一个模块中，也是允许的。
4. 要有必要的注释，注释风格采用 google-style 风格，且为中文注释。